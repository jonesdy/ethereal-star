language: cpp

compiler:
  - gcc

before_script:
  - echo 'yes' | sudo apt-add-repository ppa:sonkun/sfml-development
  - sudo apt-get update -qq
  - sudo apt-get install -y libsfml-dev
  - mkdir bin
  - cd bin
  - cmake ..


script:
  - make
